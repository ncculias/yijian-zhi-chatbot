# 變更紀錄(Changelog)

本專案所有重要變更都記錄在此檔案。

格式依循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/),
版本號依循 [語意化版本](https://semver.org/lang/zh-TW/):
修 bug 進第三位(1.0.x)、新增功能進第二位(1.x.0)、重大改版進第一位(x.0.0)。

分類標籤:`Added` 新增、`Changed` 變更、`Fixed` 修正、`Removed` 移除。

## [未發布]

<!-- 已合併但尚未部署上線的變更寫在這裡,部署時移到新版本號下 -->

## [1.1.0] - 2026-09-24

### Fixed

- 輸入框「夷間志」誤植修正為「夷堅志」(#3)
- `.gitignore` 翻譯檔白名單寫法修正,使 zh-TW.json 可正常追蹤(#3)
- 回答一律使用臺灣繁體中文,不再混入簡體字與非必要英文(#5)
- 深入問題(為什麼、意涵)不再只複述情節,會提出標明為詮釋的分析(#6)

### Added

- 首頁建議問題:16 題題庫分為主題檢索、單一條目、詮釋分析、人物主題四類,
  每次開啟新對話各類抽一題並打亂順序(共 256 種組合),四種問法必定都示範得到(#1)
- 回答結尾附「你可以接著問」延伸問題,以按鈕呈現,點擊即直接送出(#1)
- 離題提問時亦提供延伸問題,藉此把話題引回本書談得到的主題(#1)
- 歡迎頁補上使用說明(#1)
- 回答的複製鍵改置於訊息右上角,不再與延伸問題按鈕混在同一排(#1)
- 支援多輪對話:保留最近 10 輪問答,追問時可依脈絡接續回答(#2)
- 追問改寫:檢索前先把「第 2 則呢?」這類問題補成完整問句再查向量庫,
  並在改寫異常時退回原句(#2)
- 工程管理文件:開發流程、PRD/ADR 範本、迴歸測試清單、本 CHANGELOG
- 交接說明文件(`docs/交接說明.md`)

### Changed

- 檢索結果改以「[資料 N] 篇名:〈某某〉/原文:……」逐則送入 prompt。
  原本以 `list[Document]` 的 repr 形式進入,十則擠成一行、夾雜 id 與 metadata,
  篇名被雜訊淹沒,模型因此容易把正文首句當成篇名(#18)
- 系統規則新增:參考資料逐則獨立、敘述情節須指明出自哪一篇、不得跨篇合併;
  另加入出處附記辨識與提問前提核對(#22)
- 回答與改寫模型改為讀取 `settings.llm_model`,可由 `.env` 的 `LLM_MODEL` 設定。
  原本硬編碼為 `gpt-4o-mini`,使該設定形同無效。預設值不變,本次上線仍為 `gpt-4o-mini`;
  是否升級見 `docs/adr/0002-回答模型升級.md`(提案中,待確認費用)
- 追問改寫規則加強:序號或代名詞須換成實際篇名,只改量詞不算完成改寫(#19)
- 延伸問題按鈕加上淡底色,提高可點擊的辨識度(#25)

### Fixed(接手後修正)

- 語料去重:〈鹽官孝婦〉〈蕪湖儲尉〉各有一份完全重複的檔案,重複條目在向量庫
  各佔兩格、命中時吃掉 k=10 的名額。重跑 `split.py` 後為 310 則並重建向量庫(#20)
- `custom.css` 的瀏覽器快取問題(#25)

## [1.0.0] - 2026-07-27

### Added

- 首次雲端部署上線:AWS Lightsail(東京)+ Cloudflare(https://yijianzhi.org、自動 HTTPS)
- 程式碼零改動部署;伺服器端僅新增 `.env` 與 `docker-compose.override.yml`(port 80 對應)
- 架構決策紀錄見 [docs/adr/0001-部署架構選擇.md](docs/adr/0001-部署架構選擇.md)
